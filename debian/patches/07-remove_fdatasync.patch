Remove calls to fdatasync (closes: #559686). 
Upstream is going to do the same in next release.
Index: sysstat/sadc.c
===================================================================
--- sysstat.orig/sadc.c	2010-09-11 22:56:56.000000000 +0200
+++ sysstat/sadc.c	2010-09-11 23:01:25.000000000 +0200
@@ -868,11 +868,13 @@
 			 */
 			do_sa_rotat = FALSE;
 
+#if 0 /* Debian bug #559686 */
 			if (fdatasync(ofd) < 0) {
 				/* Flush previous file */
 				perror("fdatasync");
 				exit(4);
 			}
+#endif
 			close(ofd);
 			strcpy(ofile, new_ofile);
 
